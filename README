
# canvas interface

canvas-interface 是一系列库，目标是方便地利用 Canvas2D 的功能开发各种图形界面工具。并且它同样应该可以很容易地迁移至类似 Skia、SDL 等图形库，
乃至其他嵌入式的图形库。也就是说，一个没有 `div` 和浏览器环境的平台。

我们希望在这个项目中解决可以在 JavaScript 层面可以解决的问题。一些更加复杂的问题，比如文本的渲染、排版和交互式编辑，需要在更底层处理。
